{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "lldb",
      "request": "launch",
      "name": "Debug executable 'tryftp'",
      "cargo": {
        "args": ["build", "--bin=tryftp", "--package=tryftp"],
        "filter": {
          "name": "tryftp",
          "kind": "bin"
        }
      },
      "args": [],
      "cwd": "${workspaceFolder}"
    },
    {
      "type": "lldb",
      "request": "launch",
      "name": "Debug unit tests in executable 'tryftp'",
      "cargo": {
        "args": ["test", "--no-run", "--bin=tryftp", "--package=tryftp"],
        "filter": {
          "name": "tryftp",
          "kind": "bin"
        }
      },
      "args": [],
      "cwd": "${workspaceFolder}"
    }
  ]
}
